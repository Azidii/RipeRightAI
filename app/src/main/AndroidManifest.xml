<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/riperight_logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/riperight_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RipeRightAI">

        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Startup / Splash -->
        <activity
            android:name=".StartupActivity"
            android:exported="true"
            android:theme="@style/Theme.RipeRightAI">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Loading / Classification Screen -->
        <activity
            android:name=".ActivityScanLoading"
            android:exported="false"
            android:theme="@style/Theme.RipeRightAI" />

        <!-- Result Screen -->
        <activity
            android:name=".ActivityScanResult"
            android:exported="true"
            android:theme="@style/Theme.RipeRightAI" />


    </application>
</manifest>
